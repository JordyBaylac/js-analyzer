
function main() {
    var pa = 67;
    try {
        // a = 34;
        console.log(pepe);
    }
    catch (error) {
        doit(error);

        pa = 34;

        function ka(pa) {
            error = 99;
            doesNotReject(45 + error + what);
            log(pa);
        }
    }

    doit(error);
    error = 45;
}